#!/usr/bin/env bash
set -euo pipefail

# Placeholder ISO build script. This will orchestrate:
#  1. Building the installer binary for host and aarch64
#  2. Populating build/airootfs with required packages and configs
#  3. Invoking mkarchiso (or equivalent) to generate the final image
#  4. Dropping artifacts into build/iso

echo "ISO build pipeline not yet implemented."
